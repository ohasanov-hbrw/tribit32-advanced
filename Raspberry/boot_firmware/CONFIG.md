# Config Files
This folder includes my files for `/boot/firmware/`.

With `cmdline.txt` you just need to add `video=HDMI-A-1:640x480M@60D` to your file. 

With `config.txt` just compare the file and change what is different. Do this before installing RetroPie with its script.